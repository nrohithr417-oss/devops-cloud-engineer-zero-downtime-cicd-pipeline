apiVersion: v1
kind: Service
metadata:
  name: novapay-payment-service
spec:
  selector:
    app: novapay-payment-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: ClusterIP